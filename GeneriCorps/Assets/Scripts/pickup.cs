using UnityEngine;

//public class pickup : MonoBehaviour
{
    //public weaponStats weaponData;

    //private void //OnTriggerEnter(Collider other)
    {
        //if (other.CompareTag("Player"))
        {
           //IPickup pickupInterface = other.GetComponent<IPickup>();
            //if (pickupInterface != null)
            {
                //pickupInterface.getWeaponStats(weaponData);
               // Destroy(gameObject);
            }
        }
    }
}
